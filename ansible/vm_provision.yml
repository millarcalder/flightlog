- name: Provision a Web Server
  hosts: webservers
  become: yes
  pre_tasks:
    - name: Load secrets
      include_vars:
        file: vault.yml
        name: secrets
  roles:
    - role: vm_provision
