events {}
http {
    server {
        listen 80;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;

        location / {
            proxy_pass http://flightlog-frontend/;
        }

        location /api/ {
            proxy_pass http://flightlog-api:5000/;
        }
    }
}
